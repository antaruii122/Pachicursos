# Registro de ejecución — Alimenta Tu Fertilidad, plataforma de cursos

Cada vez que se cierra una Parte del roadmap (ver `docs/cursos.md`), se agrega una línea acá con el formato:

`✅ Parte X completada — YYYY-MM-DD — [resumen breve de lo construido]`

Si el subagente revisor (`curso-platform-reviewer`) rechaza el cierre de una Parte, se registra también:

`❌ Parte X rechazada — YYYY-MM-DD — [motivo del rechazo]`

**Este archivo es un historial, no un estado editable.** No se borra ni se reescribe nada de lo ya agregado — las correcciones se registran como eventos nuevos, no reemplazando los anteriores. Cualquier sesión de Claude Code debe leer este archivo completo antes de tocar código, para saber exactamente dónde retomar (ver `CLAUDE.md`).

## Roadmap de referencia (Partes A–H, detalle completo en `docs/cursos.md`)

- [ ] Parte A — Setup de cuentas/infra
- [ ] Parte B — Modelo de datos + Auth
- [ ] Parte C — Plantilla de landing + shell del reproductor + widget mínimo de subida + curso placeholder
- [ ] Parte D — Integración Vimeo segura
- [ ] Parte E — Panel de administración completo
- [ ] Parte F — Pagos
- [ ] Parte G — Consistencia de marca + QA + lanzamiento
- [ ] Parte H — Automatizaciones en n8n (boleta electrónica + emails)

## Estado actual: Parte A casi cerrada, sitio real desplegado y funcionando en producción

**Parte A — Setup de cuentas/infra** (falta solo DNS/Resend/Flow.cl):
- [x] Cuenta Supabase creada, proyecto activo, credenciales en Vercel.
- [x] Cuenta Vimeo con credenciales de API en Vercel — **falta confirmar que el plan contratado es Standard o superior** (requisito para restricción de dominio, bloquea Parte D si no).
- [x] **Proyecto Vercel creado por Ricardo, conectado al repo de GitHub (`antaruii122/Pachicursos`), deployado y funcionando en `https://pachicursos.vercel.app`.** El deploy inicial daba 500 en todo el sitio — diagnosticado y arreglado en esta sesión (ver "Incidente" más abajo). Las 7 variables de entorno reales están cargadas en Vercel (Production + Preview). Falta solo apuntar el DNS del subdominio real `cursos.alimentatufertilidad.com` — el criterio técnico de "credenciales en Vercel" ya está cumplido.
- [ ] Cuenta Resend — no iniciada.
- [ ] Trámite de cuenta de comercio Flow.cl — no iniciado.

**Incidente resuelto (2026-09-11): sitio en producción caído.**
Ricardo creó el proyecto Vercel y conectó el repo — el deploy compiló bien pero el sitio entero daba "Internal Server Error" (el `proxy.ts`/middleware corre en cada request y crashea si `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` faltan). Diagnóstico hecho con `vercel logs --json` (CLI de Vercel, ya autenticado localmente como Ricardo — se linkeó el proyecto con `vercel link`): **las 19 variables de entorno existían en Vercel pero todas tenían valor `""` vacío** — se habían creado los nombres sin pegar los valores. Se corrigió sacando y volviendo a cargar las 7 variables reales (de `.env.local`) vía `vercel env rm`/`vercel env add`, y se redeployó (`vercel --prod`). Verificado en vivo: home y landing del curso responden 200 con contenido real.
De paso, dos gaps más encontrados y arreglados en el mismo sitio real:
- `/` todavía era la plantilla default de `create-next-app` ("To get started, edit page.tsx") — nunca se había construido un home real. Se reemplazó por un catálogo de cursos publicados.
- Las páginas de auth (`/cuenta/login`, `registro`, `olvide-password`, `actualizar-password`) no tenían header ni logo — se construyeron en la Parte B antes de que existiera `SiteHeader`. Se separaron en página servidor (con header/footer) + componente cliente del formulario, y se migraron del hex hardcodeado a los tokens de `globals.css`.

**Parte B — Modelo de datos + Auth** (adelantada a pedido explícito de Ricardo, antes de cerrar Parte A):
- [x] `supabase/migrations/0001_init.sql` — las 9 tablas del modelo de datos, constraints, índices y RLS. Aplicada en el proyecto Supabase real.
- [x] `supabase/migrations/0002_profiles_role_column_protection.sql` — fix de un bug real encontrado al escribir el test de acceso cruzado: la policy de update de `profiles` permitía auto-promoción a admin (RLS no protege columnas). **Corrida en el proyecto Supabase real (confirmado por Ricardo).** Como consecuencia, cambiar `role` ya no se puede hacer desde el cliente ni por un admin — solo con la service_role key (ver `scripts/set-admin-role.mjs` abajo).
- [x] `scripts/set-admin-role.mjs` (`npm run set-admin -- correo@ejemplo.com`) — promueve una cuenta ya registrada a `role=admin` usando la service_role key (necesario después de 0002). **Todavía no corrido** — Ricardo tiene que registrar una cuenta normal primero en `/cuenta/registro`.
- [x] `src/lib/supabase/client.ts` y `server.ts` — helpers oficiales de `@supabase/ssr` (patrón verificado contra el repo oficial de ejemplos de Next.js, no adivinado).
- [x] `src/proxy.ts` — refresco de sesión únicamente (Next.js 16 renombró "middleware" a "proxy" — el codemod oficial también renombra la función exportada, ya aplicado). Sin redirecciones globales todavía; el gate de acceso a clases específicas ya se implementó directo en la Parte C (ver abajo), no acá.
- [x] Páginas de auth en español: `/cuenta/login`, `/cuenta/registro`, `/cuenta/olvide-password`, `/cuenta/actualizar-password`, `/cuenta/logout`, `/auth/confirm` (route handler), `/auth/error`.
- [ ] **Pendiente manual en el dashboard de Supabase**: editar las plantillas de email (Authentication → Email Templates) para que los links de confirmación/recuperación usen el patrón `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type={{ .Type }}&next=/` — la plantilla default no pasa por nuestra ruta `/auth/confirm`.
- [ ] `scripts/test-rls-cross-access.mjs` — escrito, corre 3 asserts (alumno no lee perfil ajeno, alumno no se auto-promueve a admin, admin sí lee todos los perfiles). **0002 ya está aplicada, así que ahora sí se puede correr** (`npm run test:rls`) — sigue pendiente porque crea/borra usuarios de prueba reales y Ricardo prefirió dejarlo para cuando quiera.

**Parte C — Plantilla de landing + shell del reproductor + widget de subida + curso placeholder** (adelantada, prácticamente completa):
- [x] `src/components/CourseLanding.tsx` — plantilla ÚNICA parametrizada, las 10 secciones del roadmap en orden fijo, replicando el mockup real (leído del canvas publicado, no de memoria: colores, copy y estructura exactos de `Main.dc.html`). Cada sección se omite si el curso no tiene ese contenido cargado (testimonios, FAQ, etc.), tal como pide el plan.
- [x] `src/app/cursos/[slug]/page.tsx` — landing real, server-rendered, con SEO metadata dinámica. Un curso no publicado da 404 automáticamente (RLS ya lo filtra, no hace falta lógica extra).
- [x] `src/app/cursos/[slug]/clase/[n]/page.tsx` — shell del reproductor con los 3 estados: gratis, bloqueada (con CTA a comprar, sin redirect), y "sin sesión intentando ver clase paga" → redirect a `/cuenta/login?next=...` con retorno preservado. **Verificado en vivo** con el curso placeholder: los 3 casos responden como corresponde (200/200/307).
- [x] `src/components/ClassNotes.tsx` — notas personales con autoguardado (debounce, sin botón guardar), una por alumno por clase vía upsert (usa la constraint `unique(user_id, video_id)` de 0001).
- [x] `scripts/seed-placeholder-course.mjs` (`npm run seed:placeholder`) — curso placeholder "Regula tu Ciclo, Recupera tu Fertilidad" con 4 clases (1 gratis), idempotente (upsert por slug). **Ya corrido contra el proyecto real** — visible en `/cursos/placeholder-regula-tu-ciclo`. Falta borrarlo/despublicarlo antes de la Parte G.
- [x] Paleta/tipografía de marca movidas a `globals.css` y `layout.tsx` (Lato/Noto Serif/Poppins vía `next/font`, los 12 tokens de color) — ya no hardcodeadas por página.
- [x] `src/lib/vimeo.ts` + `src/app/api/vimeo/upload-url/route.ts` — endpoint que crea el upload TUS en Vimeo (`POST /me/videos`, `approach: tus`) y guarda `vimeo_id`. Patrón verificado contra el SDK oficial `vimeo/vimeo.py` (no adivinado): headers `Accept: application/vnd.vimeo.*;version=3.4`, extracción del id desde `uri`. Valida sesión (401) y `role=admin` server-side (403) antes de llamar a Vimeo — **verificado en vivo** que ambos gates responden correctamente sin sesión.
- [x] `src/components/VideoUploadWidget.tsx` — sube el archivo directo navegador → Vimeo con `tus-js-client` (opción `uploadUrl`, confirmada en la doc oficial del paquete como el patrón recomendado para APIs que ya crean el recurso de subida, como Vimeo) — nunca pasa por nuestro servidor. Barra de progreso durante la subida; después hace polling de estado (ver Parte D abajo).
- [x] `src/app/admin/subir-video-prueba/page.tsx` — página mínima para probar el pipe (no es el panel admin real, eso es Parte E), protegida por sesión + rol admin.
- [ ] **No probado con un archivo real todavía — y ahora sabemos por qué probablemente fallaría**: ver el hallazgo del token de Vimeo sin scope "upload" en la Parte D. Hasta que se regenere el token, ni Ricardo subiendo un video real desde `/admin/subir-video-prueba` va a funcionar. El build/lint pasan y los gates de seguridad están verificados; solo falta la prueba end-to-end, y ahora hay un bloqueante identificado en el camino.
- [x] **Bug real encontrado por Ricardo en producción (2026-09-12) y arreglado**: la fila de la clase gratis en la sección 5 ("N clases, a tu ritmo") tenía ícono de play, borde rojo y badge "Gratis" — todas las señales visuales de que se puede hacer click — pero era un `<div>` sin `href` ni `onClick`, no hacía nada al tocarla. La miniatura grande de la sección 3 (embed de la clase gratis) sí funcionaba bien; solo faltaba este segundo punto de entrada. Ahora enlaza a `/cursos/[slug]/clase/[n]` igual que la miniatura. Las filas bloqueadas quedaron igual (nunca tuvieron ícono de play ni borde, no invitaban a hacer click).
- [x] **Decisión tomada (2026-09-11): landing mobile muestra las 10 secciones, no la versión condensada del artboard mobile del mockup** — se le preguntó explícitamente a Ricardo porque el mockup mobile omite instructor/testimonios/FAQ, lo cual contradice el criterio "mobile-first" del propio plan (la mayoría del tráfico es celular, y esas secciones son las que generan confianza para comprar). Se confirmó que el código actual de `CourseLanding.tsx` ya reflowa correctamente a una columna en mobile (grid sin cols hasta el breakpoint `md:`, tarjetas con `flex-wrap`) — no hizo falta cambiar código, solo confirmar el alcance. MisCursos/AdminCurso del mockup siguen sin leerse en detalle — son Parte E, no bloquean Parte C.
- No se ha invocado el subagente `curso-platform-reviewer` — no se cierra ninguna Parte formalmente hasta que lo apruebe.

**Parte D — Integración Vimeo segura** (adelantada, código completo, falta solo probar con un video real):
- [x] **Corrección importante al plan (2026-09-11)**: `docs/cursos.md` asumía que la transición `procesando→listo` se detecta "con el webhook de Vimeo (con polling como respaldo)". Investigado y confirmado que **Vimeo no tiene webhook de fin de transcoding** — limitación real de su API, documentada en múltiples issues de sus propios SDKs oficiales, nunca resuelta. El plan quedó corregido (acá y en el `cursos.md` original fuera del repo, ambos sincronizados) para reflejar que el polling a `transcode.status` es el único mecanismo real.
- [x] **Decisión tomada con Ricardo**: el polling se hace desde el navegador del admin mientras tiene la página de subida abierta (no cron/n8n, no chequeo perezoso en el próximo page load) — más simple, sin infraestructura nueva, aceptando que se corta si cierra la pestaña antes de que termine.
- [x] `getVimeoTranscodeStatus()` en `src/lib/vimeo.ts` + `src/app/api/vimeo/status/[videoId]/route.ts` — consulta `GET /videos/{vimeo_id}?fields=transcode.status`, actualiza `estado_procesamiento` a `listo` cuando corresponde. Mismos gates de sesión+admin que `/api/vimeo/upload-url`, **verificado en vivo** (401 sin sesión).
- [x] `VideoUploadWidget.tsx` ahora hace polling cada 5s tras terminar la subida (tope de 60 intentos / ~5 min), con estados visuales subiendo→procesando→listo.
- [x] **Bug real encontrado por Ricardo (2026-09-12)**: preguntó si los "12 min" que se ven en el curso placeholder salen del video real o están hardcodeados. Estaban hardcodeados (dato de mentira del script de seed) — pero el hallazgo más importante es que **no existía ningún mecanismo, en ningún lado del código real, que alguna vez escribiera `course_videos.duracion`**: ni un campo en el form de "agregar clase" del admin, ni el polling de Vimeo. Todo curso real iba a quedar sin badge de duración para siempre. Arreglado en la fuente: `/api/vimeo/status/[videoId]` ahora pide el campo `duration` de Vimeo en la misma consulta de `transcode.status`, y lo graba en `course_videos.duracion` apenas el procesamiento queda "listo" — la duración real del video, nunca tipeada a mano.
- [x] **`player_embed_url` verificado sin poder usar la doc oficial de Vimeo** (developer.vimeo.com es una SPA que bloquea el scraping automático) — confirmado revisando uso real en decenas de repos de producción en GitHub, incluido el extractor de `yt-dlp`. Confirmado también que ese campo ya incluye el hash de privacidad para videos "unlisted", así que nunca se arma la URL a mano.
- [x] `getVimeoEmbedUrl()` en `src/lib/vimeo.ts` + `src/app/api/courses/[slug]/videos/[videoId]/player/route.ts` — el endpoint central de seguridad del plan: valida acceso (gratis o compra pagada, con `service_role` solo para leer `vimeo_id` DESPUÉS de validar) y recién ahí pide el `player_embed_url` a Vimeo. **Verificado en vivo contra el proyecto real** con las 4 clases placeholder: clase paga sin sesión → 401; clase gratis con `estado_procesamiento='listo'` pero sin `vimeo_id` real (nunca se subió nada) → 404 "todavía no tiene video subido", sin filtrar nada. Comportamiento correcto en ambos casos.
- [x] `src/components/ClassPlayer.tsx` — pide el embed al endpoint de arriba y renderiza el iframe real; reemplaza el placeholder visual de la Parte C. Ya integrado en `/cursos/[slug]/clase/[n]`.
- [x] **Probado de punta a punta contra la API real de Vimeo (2026-09-12), sin necesitar sesión de admin en el navegador**: `scripts/test-vimeo-upload.mjs` (`npm run test:vimeo -- <video.mp4>`) sube un archivo real vía TUS, hace polling de `transcode.status`, pide el `player_embed_url`, y borra el video de prueba al final — mismos endpoints que `src/lib/vimeo.ts`, contra la cuenta real, no un mock.
- [ ] **🔴 Bloqueante real encontrado**: el test falló en el primer paso con `403 — "Your access token does not have the 'upload' scope"`. El `VIMEO_ACCESS_TOKEN` que está hoy en `.env.local` (y probablemente en Vercel, son el mismo valor) **no tiene el scope "upload"** — sin este scope, la función `createVimeoTusUpload()` nunca puede funcionar, para nadie, ni siquiera con sesión de admin real. Esto significa que la subida de video **todavía no puede probarse con un archivo real hasta que se regenere el token** — no es que falte que Ricardo se loguee, es un permiso mal configurado en el token mismo.
  - **Cómo arreglarlo** (`.env.example` ya documentaba que hacían falta los scopes "upload", "edit", "private" — el token actual no los tiene todos): entrar a `developer.vimeo.com/apps` → la app de Alimenta Tu Fertilidad → "Generate an access token" → tildar explícitamente **Upload**, además de Edit y Private → generar un token nuevo → reemplazar `VIMEO_ACCESS_TOKEN` en `.env.local` **y** en Vercel (Production + Preview) → redeploy (`vercel --prod`, porque aunque no es `NEXT_PUBLIC_*` los cambios de env vars igual requieren un nuevo deploy para tomar efecto en las funciones serverless).
- No se ha invocado el subagente revisor — no se cierra la Parte.

**Parte E — Panel de administración completo** (código completo, falta probar en vivo):
- [x] `src/app/admin/layout.tsx` — gate único de sesión+admin para todo `/admin/**` (antes cada página lo hacía por separado). **Verificado en vivo**: `/admin/cursos` y `/admin/cursos/nuevo` redirigen a login sin sesión.
- [x] `src/app/admin/cursos/actions.ts` — server actions `saveCourse`/`setCourseEstado`/`deleteCourse`, todas re-validan `role=admin` server-side además de RLS (mismo criterio que los endpoints de Vimeo). `deleteCourse` bloquea el borrado si el curso ya tiene compras (reales o manuales) — decisión propia, no estaba explícita en el plan, para no perder el registro de ventas/accesos; sugiere "Archivar" en su lugar.
- [x] `src/app/admin/cursos/page.tsx` — lista de cursos con estado y precio.
- [x] `src/components/admin/CourseForm.tsx` — formulario único para crear/editar (todos los campos de contenido de venta: promesa, para quién es/no es, qué vas a aprender, FAQ, testimonios, SEO, imágenes por URL). Listas dinámicas (agregar/quitar) para qué-vas-a-aprender/FAQ/testimonios. Slug autogenerado del título hasta que el admin lo edita a mano.
- [x] Indicador de completitud (`calcularCompletitud` en `lib/types.ts`) — bloqueantes (título, precio, ≥1 clase, todas las clases en "listo") vs. advertencias (FAQ/testimonios/qué-vas-a-aprender vacíos). El botón "Publicar" queda deshabilitado mientras haya bloqueantes.
- [x] Publicar/despublicar/archivar/borrar, con confirmación en borrar.
- [x] `src/app/admin/cursos/[id]/preview/page.tsx` — vista previa reutilizando `CourseLanding` (la misma plantilla, nunca ad-hoc), funciona sin importar el estado del curso.
- [x] **Gap encontrado**: otorgar acceso manual necesita buscar un alumno por email, pero `profiles` nunca guardaba el email (vive en `auth.users`, que el cliente no puede leer). `supabase/migrations/0003_profiles_add_email.sql` agrega la columna, hace backfill, y actualiza el trigger de registro para poblarla siempre. **Todavía no corrida en el proyecto real** — hace falta antes de que "Otorgar acceso" funcione.
- [x] `src/app/admin/cursos/[id]/clases/` — gestor de clases: agregar (con orden automático), marcar como gratis (desmarca la anterior sola, respeta la constraint de "máximo 1 gratis"), reordenar con botones ▲▼ (swap con valor temporal para no chocar contra `UNIQUE(course_id, orden)` a mitad de camino — **decisión propia**: se usaron botones en vez de arrastrar-y-soltar con mouse para no sumar una librería de drag-and-drop nueva; el resultado funcional es el mismo, reordenar sin tocar la base de datos a mano), borrar (con aviso explícito de que se pierden las notas de los alumnos, como pide el plan). Reutiliza el `VideoUploadWidget` ya construido en C/D tal cual — subir a una clase existente YA ES "reemplazar el video sin borrar la clase", no hizo falta lógica nueva.
- [x] `src/app/admin/cursos/[id]/accesos/` — otorgar acceso manual (busca por email, crea `purchases` con `proveedor_pago=manual`, `monto=0`), botones separados "Revocar" (sin devolución) vs "Marcar reembolsado" (con aviso de que la devolución real se hace aparte en Flow.cl/Stripe, tal cual pide el plan).
- [ ] **Gap conocido, no bloqueante**: el plan pide avisarle por email al alumno cuando se le otorga acceso manual — no implementado todavía porque depende de la cuenta de Resend (Parte A, no creada). Queda con un TODO explícito en el código (`accesos/actions.ts`). El acceso se otorga igual, solo falta el email de aviso.
- [x] `src/app/admin/ventas/page.tsx` — historial de ventas/accesos de todos los cursos (alumna, curso, monto, vía, estado, fecha).
- [x] `src/app/legal/{terminos,privacidad,cookies}/page.tsx` — estructura publicada y enlazada desde el footer (ya no dan 404). Contenido explícitamente marcado `[PLACEHOLDER]` — el texto real lo escribe Marcela/asesor legal, no es trabajo de desarrollo (tal cual dice el plan).
- **Verificado en vivo**: `/admin/ventas` redirige a login sin sesión; las 3 páginas legales cargan público sin login (200).
- No probado en vivo con sesión de admin real todavía (mismo bloqueo de siempre) — el flujo completo (crear curso → agregar clases → subir video → publicar → otorgar acceso) sigue sin probarse de punta a punta.
- No se ha invocado el subagente revisor — no se cierra la Parte.

**Gap grande encontrado y cerrado (2026-09-12), fuera de cualquier Parte específica: `lesson_progress` nunca se escribía desde ningún lado.**
Investigando la pregunta de Ricardo sobre la duración hardcodeada, se encontró algo más grande: **ningún código de la app escribía jamás en `lesson_progress`** — ni un botón "marcar como vista", ni un tracker automático, nada. Esa tabla es la que respalda dos cosas que el plan pide explícitamente: la barra de % de avance en "Mis cursos" y el botón "Continuar" (llevar al alumno a la última clase que estaba viendo). Las dos estaban muertas para siempre: todo curso iba a mostrar 0% por siempre, y "Continuar" siempre iba a mandar a la clase 1 porque nunca había actividad real para ordenar.
- [x] Se agregó `@vimeo/player` (SDK oficial de Vimeo, MIT, la forma correcta de leer el estado real de reproducción de un iframe embebido — se prefirió sobre reimplementar a mano su protocolo de postMessage, que sí sería "código contra una API adivinada"). `ClassPlayer.tsx` ahora escucha `timeupdate` (guarda `progress_seconds` cada 10s como máximo, no en cada evento), marca `completed=true` al pasar 90% visto o al evento `ended` (lo que pase primero — muchos alumnos nunca llegan al frame exacto final), y retoma la posición guardada al reabrir la clase.
- [x] `lesson_progress` ya tenía una policy RLS permisiva por alumno (mismo modelo de confianza que `video_notes` — progreso autoreportado, sin implicancia de acceso), así que esto escribe directo desde el cliente igual que las notas.
- [ ] Sigue sin probarse con reproducción real (mismo bloqueante del token de Vimeo sin scope "upload" — sin poder subir un video real, no se puede confirmar en vivo que el guardado de progreso funciona con un player real, solo que compila y no rompe nada existente).

**Gap encontrado y cerrado fuera de la Parte E: "Mis cursos" nunca se había construido.**
El flujo de compra descrito en `docs/cursos.md` (paso 7) exige un dashboard "Mis cursos" — no dependía de que existieran pagos reales (Parte F), porque los accesos manuales de la Parte E ya generan filas `purchases` reales. Se construyó ahora en vez de dejarlo para más adelante:
- [x] `src/app/cuenta/mis-cursos/page.tsx` — cards por curso comprado con % de avance (calculado al vuelo desde `lesson_progress`, sin tabla nueva, tal cual pide el plan), botón "Continuar" a la última clase con actividad (o la clase 1 si nunca empezó), "Repasar" si ya completó el curso. **Verificado en vivo**: redirige a login sin sesión.
- [x] `src/components/SiteHeader.tsx` ahora es auth-aware (antes era estático): muestra "Mis cursos"/"Cerrar sesión" logueado, "Iniciar sesión" si no. **Verificado en vivo** en la landing pública.
- [x] **Bug real encontrado al construir esto, no solo en la pantalla nueva**: las policies de SELECT de `courses` y `course_videos` (0001) solo dejaban ver un curso si estaba `publicado` o quien consulta es admin — un alumno que compró un curso que después se despublica quedaba bloqueado por RLS, tanto en "Mis cursos" como en el reproductor de clase ya existente desde la Parte C/D. Esto contradice directamente el plan ("sigue apareciendo normal... sin candado ni aviso de error"). `supabase/migrations/0004_courses_purchased_visibility.sql` lo corrige agregando "o tiene una compra pagada" a ambas policies. **Todavía no corrida en el proyecto real.**
- [x] Limpieza: `/admin/subir-video-prueba` tenía su propio chequeo de sesión+admin y su propio `SiteHeader` de cuando se construyó, antes de que existiera `admin/layout.tsx` — quedaba con header duplicado y el chequeo repetido. Sacado, ahora depende solo del layout.

**Parte F — Pagos** (Flow.cl construido y verificado contra el sandbox real; Stripe no empezado):
- [x] `src/lib/flow.ts` — cliente de la API de Flow.cl (firma HMAC-SHA256, `payment/create`, `payment/getStatus`). Patrón de firma y endpoints verificados contra una implementación real ya validada en sandbox (developers.flow.cl es una SPA que bloquea scraping automático) — no adivinado.
- [x] `src/app/checkout/[slug]/page.tsx` — crea la fila `purchases` en "pendiente" con `user_id` ya guardado ANTES de llamar a Flow (tal cual pide el plan, así el webhook nunca depende de que la sesión del navegador siga viva), redirige a la pasarela. Curso ya comprado → redirige directo a la clase 1 en vez de cobrar de nuevo.
- [x] `src/app/checkout/retorno/page.tsx` — pantalla post-pago (`urlReturn`), re-consulta el estado a Flow con nuestra propia firma (nunca confía en la URL de retorno), con botón "Reintentar pago" si falló.
- [x] `src/app/api/webhooks/flow/route.ts` — **hallazgo importante**: Flow no manda un payload firmado a `urlConfirmation`, manda solo `{ token }`. La verificación real es volver a pedir `payment/getStatus` con nuestra propia firma — no hay un header de firma que verificar como en Stripe. Idempotente: un estado terminal (`pagado`/`fallido`/`reembolsado`/`revocado`) nunca se vuelve a tocar sin importar cuántas veces Flow reintente. Cada llamada queda logueada en `payment_webhook_events`.
- [x] **Verificado en vivo contra el sandbox real de Flow** (no solo build/lint): un token falso hace que nuestra firma llegue hasta la API real de Flow y la API la rechaza limpiamente (502 controlado) — confirma que el algoritmo de firma funciona de punta a punta contra su servidor real, no solo en teoría.
- [x] Bug encontrado y arreglado en el momento: `request.formData()` tiraba una excepción sin capturar (500 feo) si el body no venía bien formado — ahora responde 400 limpio.
- [ ] **Falta**: emails transaccionales (confirmación de compra, bienvenida, aviso de venta a Marcela) — bloqueados en Resend (Parte A, sin cuenta todavía), quedan como TODO explícito en el código. Evento `Purchase` de Meta Pixel — Parte G. **Stripe (proveedor secundario) no se construyó todavía.**
- No probado con un pago real de punta a punta (necesita que el trámite de comercio de Flow.cl esté aprobado, todavía sin iniciar).
- No se ha invocado el subagente revisor.

**Estado de las cuentas de prueba (2026-09-12)**: ya hay 2 cuentas reales con `role=admin` en el proyecto — `rcgiroz@gmail.com` (cuenta vieja de antes de este proyecto; no tenía fila en `profiles`, se le creó una) y `Antario@gmail.com` / contraseña `5320` (creada directo con la service_role key, email ya confirmado). Ricardo ya entró al panel admin real (`/admin/cursos`) — el problema inicial era que el header no mostraba ningún link al panel admin, ya arreglado (`SiteHeader` ahora muestra "Panel admin" si `role=admin`).

**⚠️ INSTRUCCIÓN EXPLÍCITA DE RICARDO (2026-09-12): NO TOCAR Flow.cl NI Stripe por 2-3 días.** Dijo textualmente que parara con "flow.cl" y construyera "todo lo demás" mientras tanto. Cualquier sesión que retome esto antes de esa fecha debe seguir evitando pagos y trabajar en otra cosa (Parte G, gaps de Parte E, etc.) — no asumir que esto ya se puede tocar de nuevo solo porque pasó algo de tiempo, confirmar con Ricardo primero.

**Parte G — Consistencia de marca + QA** (arrancada, no completa — por instrucción explícita de no tocar pagos):
- [x] Imágenes de portada de curso pasadas de `<img>` a `next/image` (landing, catálogo home, Mis cursos) — el plan pide explícitamente imágenes optimizadas (WebP/AVIF vía CDN de Vercel), nunca el archivo crudo; antes se violaba esto en 3 lugares.
- [x] Accesibilidad: outline de foco visible en toda la app (antes no existía en ningún lado), todos los pares label/input del formulario de curso + gestor de clases + otorgar-acceso ahora están asociados de verdad (`htmlFor`/`id`, o `aria-label` en los campos de listas dinámicas que no pueden tener un id fijo), mensajes de error con `role="alert"`, texto de estado async (autoguardado de notas, progreso de subida) con `role="status"`/`aria-live`.
- [x] **Auditoría real con Lighthouse (2026-09-12)** contra producción (`https://pachicursos.vercel.app`, mobile): Performance 88 (home) / 93 (landing), Best Practices 100, SEO 100, Accessibility 98 (home) / 93 (landing). CLS 0 en ambas, TBT <100ms — ningún problema de layout shift ni de JS bloqueante.
- [x] **2 bugs reales de accesibilidad encontrados y arreglados** a partir de esa auditoría (no es trabajo de pagos, no toca `checkout/*`):
  - Ninguna página tenía un landmark `<main>` (Lighthouse: "Document does not have a main landmark"). Agregado en las 9 páginas/plantillas que renderizan `SiteHeader`+contenido+`SiteFooter`: home, `CourseLanding`, Mis cursos, `clase/[n]`, las 4 páginas de auth, y `LegalPage`. **Deliberadamente NO tocado en `checkout/[slug]` ni `checkout/retorno`** — quedan fuera del alcance de esta sesión por la instrucción explícita de no tocar nada de pagos, aunque el cambio ahí hubiera sido igual de trivial.
  - Contraste insuficiente en `CourseLanding.tsx`: el badge de duración sobre la miniatura de video (texto blanco sobre `bg-black/35`) daba 2.43:1, subido a `bg-black/65` (verificado 7:1 incluso contra una miniatura blanca en el peor caso). El badge de duración de las clases bloqueadas heredaba `opacity-85` de la tarjeta contenedora, lo que bajaba el contraste del texto de 5.2:1 a 4.02:1 (por debajo del mínimo 4.5:1) — se sacó el `opacity-85` y se reemplazó por un fondo sólido más tenue (`--crema-2`) para distinguir visualmente las clases bloqueadas sin tocar el contraste del texto.
  - **Verificado**: landing page pasó de 93 a 100 en Accessibility (Lighthouse local contra build de producción, cero fallos restantes).
- [x] **Auditoría estática de navegación por teclado (2026-09-12)**: no hay ninguna herramienta de browser automation disponible en esta sesión para probarlo en vivo con un teclado real, así que se hizo una auditoría de código en su lugar. Se confirmó que **todos** los `onClick` de la app viven en un `<button type="button">` real (cero `div`/`span` clicable sin soporte de teclado) y que no hay ningún `tabIndex` positivo en todo el código (el orden de tabulación sigue el orden natural del DOM, no hace falta arreglar nada ahí). El único gap real encontrado: no había forma de saltar la navegación del header para llegar al contenido. Se agregó un link "Saltar al contenido" (oculto visualmente, aparece al enfocarlo con Tab) como primer elemento enfocable de `SiteHeader`, apuntando a `id="contenido-principal"` en cada `<main>` agregado antes. **Sigue pendiente probarlo con un teclado real** (esta auditoría solo confirma que el código no tiene los bugs típicos, no reemplaza la prueba manual).
- [x] **Auditoría de consistencia de marca (2026-09-12)**: sin el mockup original a mano para comparar pixel a pixel (se leyó del canvas publicado en una sesión anterior, no es un archivo local), se hizo en su lugar una auditoría de código — colores hardcodeados fuera del sistema de tokens, y páginas que nunca recibieron el header/footer compartido. Encontrado y arreglado:
  - `/auth/error` y `/cuenta/logout` **nunca tuvieron `SiteHeader`/`SiteFooter`** (quedaron de antes de que existieran) — un alumno con un link de confirmación roto o vencido veía una página completamente sin marca, sin logo ni nav. Además usaban colores a mano en vez de los tokens; uno de ellos (`#3B1420`) ni siquiera coincide con ningún color real de la paleta. Arreglado en ambas; `/cuenta/logout` tuvo que separarse en página servidor + `LogoutRedirect.tsx` cliente, porque un componente cliente no puede renderizar directo un Server Component async como `SiteHeader` (error real de build, no solo estilo).
  - **El favicon nunca se tocó desde que se creó el proyecto** (confirmado con `git log --follow`) — la pestaña del navegador mostraba el ícono default de Next.js, no ninguna marca de Alimenta Tu Fertilidad. Se agregó `icon.svg` reusando el mismo corazón del logo de `SiteHeader`.
  - **No existía metadata de Open Graph/Twitter Card en ningún lado** — cualquier link del sitio compartido por WhatsApp o Instagram (el canal de venta real del negocio) no mostraba ninguna vista previa. Se agregó `openGraph`/`twitter` en el layout raíz y, por curso, usando la `cover_image_url` real que ya carga el admin (si el curso no tiene imagen todavía, cae a una tarjeta simple en vez de inventar una imagen — el curso placeholder hoy no tiene, y se comporta así correctamente).
- [ ] Falta: revisión lado a lado con el mockup visual original (no disponible localmente esta sesión), instalar Meta Pixel (bloqueado en cuenta Meta Business, no creada), prueba manual real con teclado (Tab/Shift+Tab/Enter de punta a punta — no hay herramienta de automatización de navegador en esta sesión), borrar/despublicar el curso placeholder antes del lanzamiento real. El score de Performance (88-93) es bueno pero no se investigó a fondo qué compone el Speed Index de 6.8s en home — no bloqueante, pero queda como posible mejora.
- No se ha invocado el subagente revisor.

## Auditoría profunda de calidad (2026-09-12) — plan en `C:\Users\rcgir\.claude\plans\eager-toasting-bird.md`

Ricardo se enojó en serio viendo un screenshot del panel de clases y pidió una auditoría completa, honesta, con 3 agentes en paralelo: ¿el admin puede resolver todo desde su panel?, ¿por qué se ve "de baja calidad" con la capacidad que tenemos?, ¿se le dio demasiada atención a pagos y muy poca a UI/UX? El análisis completo (con cita de archivo/línea de cada hallazgo) vive en el archivo de plan de arriba — acá el resumen de lo que se encontró y lo que ya se corrigió.

**Veredicto honesto sobre pagos vs. UI/UX** (verificado con `git log`, no es una opinión): Flow.cl fue el bucket de trabajo más chico de todo el proyecto (1 commit, ~424 líneas) — no le sacó tiempo a la UI. La causa real es otra: nadie miró el sitio en producción "con ojos de visitante" hasta que un incidente no relacionado (variables de entorno vacías) forzó a hacerlo — y ahí se encontró que el home todavía era la plantilla default de Next.js. El mismo patrón se repitió: los bugs reales aparecen cuando alguien de verdad usa el sitio, no en una pasada de QA planificada. Esto ya se corrigió una vez (Parte A/G temprano) pero el patrón de fondo seguía sin resolverse hasta esta auditoría.

**Encontrado y ya corregido en esta sesión, a partir de la auditoría**:
- [x] **Bug real de pérdida de datos**: `CourseForm.tsx` inicializaba `seo_titulo`/`seo_descripcion` siempre vacíos (ni siquiera se pedían en el SELECT de la página de editar) — guardar cualquier curso existente borraba silenciosamente su SEO guardado. Corregido: el tipo `Course`, el SELECT, y el estado inicial del form ahora sí leen estos campos.
- [x] **El único curso publicado tenía "[PLACEHOLDER]" literal en el título**, visible en el `<title>` real, en el Open Graph (cualquier link compartido por WhatsApp mostraría "[PLACEHOLDER]" en la vista previa), y en la card del home. Corregido en el script de seed y vuelto a correr contra la base real — verificado en vivo, ya no aparece en ningún lado público.
- [x] **El hallazgo central de Ricardo, diagnosticado**: el reproductor de clase no tenía ninguna navegación entre clases — la única forma de pasar de la clase 3 a la 4 era volver a la landing de venta y bajar hasta el currículum. Se construyó `ClassSidebar.tsx`: lista de clases con check de completada, clase actual resaltada, candado en las bloqueadas — colapsable en mobile (`<details>`, mismo patrón que el acordeón de FAQ), fija en desktop. Esto es exactamente lo que toda plataforma de cursos real (Teachable/Kajabi/Thinkific) hace y lo que faltaba acá.
- [x] **Bug de contraste app-wide encontrado auditando la página nueva**: `--dorado-osc` (el único color de error/warning de toda la app, usado en 14 lugares) daba 3.38:1 sobre blanco — insuficiente. Oscurecido a nivel de token (ahora 5.44:1), corrige los 14 usos de una sola vez.

**Todavía pendiente del plan** (ver el archivo de plan para el detalle completo): admin no puede subir fotos, solo pegar URLs externas (recomendado: Supabase Storage, ya pagado, sin cuenta nueva); no hay miniaturas de video en ningún lado; 3 columnas modeladas en la base y nunca conectadas a ninguna UI (`accent_color`, `seo_og_image`, `course_videos.resources` — necesita que Ricardo decida si se construyen o se borran); falta un sistema de diseño real (casi no hay `transition`/`animate` en toda la app, radios/sombras tipeados a mano por componente); el panel admin en sí se ve como CRUD interno, no como un dashboard (sin tarjetas de KPI, sin miniaturas, `confirm()` nativo del navegador); falta el patrón de carga "facade" del video (imagen + botón play antes de cargar el iframe pesado) que el plan original pedía y nunca se construyó; las 3 páginas legales siguen con texto `[PLACEHOLDER]`/`[FECHA]` literal (esto es contenido de Marcela/asesor legal, no desarrollo). El token de Vimeo sin scope "upload" (Parte D) sigue bloqueando probar un video real de punta a punta.

## QA visual real contra producción (2026-09-13)

Ricardo pidió, con razón, que se revisara el sitio de verdad — con un navegador, no solo leyendo código. Se armó un setup de screenshots reales: Chrome headless (el binario real de Windows, `C:\Program Files\Google\Chrome\Application\chrome.exe`) manejado con `puppeteer-core` (instalado aislado en un directorio de scratch, nunca se tocó el `package.json` del proyecto), con un login real como admin (`Antario@gmail.com`) contra `https://pachicursos.vercel.app`. Se navegó y capturó: home, landing, reproductor de clase (logueado y no), "Mis cursos", y 5 pantallas del panel admin (lista de cursos, editor, gestor de clases, accesos, ventas), en desktop y mobile.

**Encontrado por esto (no visible solo leyendo código) y ya corregido**:
- [x] **El header no tenía NINGÚN manejo responsive** (cero breakpoints) — en un celular real se veía partido en dos líneas apretadas, con el subtítulo de marca y los links de nav chocando entre sí. Arreglado: subtítulo y el link "volver al sitio principal" se ocultan bajo `sm:`, tipografía y espaciado se ajustan en mobile.
- [x] **Los estados "procesando" y "sin video" del reproductor eran solo texto plano flotando en un cuadro blanco enorme** — comparado con el estado "bloqueada" (que sí tiene ícono + texto + botón), se veían rotos/incompletos. Se les agregó un ícono a juego con el mismo lenguaje visual.
- [x] **Confirmado visualmente que el sidebar de clases (arreglado hoy más temprano) funciona perfecto** — colapsable en mobile, fijo en desktop, exactamente como se diseñó.

**Confirmado visualmente (ya sabíamos por el código, pero ahora hay captura real de pantalla como prueba)**: el home, la landing, y el editor de curso muestran cajas de gradiente genéricas con un ícono tenue en cada lugar donde debería ir una foto real (portada del curso, foto de Marcela, miniatura de la clase gratis) — porque el campo es un input de texto para pegar una URL, no un botón de subida. Esto es lo que Ricardo señaló desde el principio y ahora hay captura de pantalla real que lo prueba, no solo el hallazgo de código. Screenshots enviados directamente a Ricardo.

## Pruebas de interacción real (2026-09-14) — Puppeteer haciendo click de verdad, no solo mirando

A pedido explícito de Ricardo ("get browser use to go into the website and tets eeyrthing"), se probaron flujos reales con clicks reales (no solo screenshots): navegación admin→clases, reordenar clases, otorgar acceso manual, login/logout repetido, crear curso nuevo.

**🔴 Encontrado — bloqueante real, no solo pendiente**: "Otorgar acceso manual" (`/admin/cursos/[id]/accesos`) **está completamente roto en producción ahora mismo**. Confirmado con una consulta directa a la base real (no un test de UI): `profiles.email` **no existe como columna** — la migración `0003` nunca se corrió. El código de `grantAccess` (`accesos/actions.ts`) hace `.eq("email", ...)` contra esa columna, la consulta tira un error de Postgres real, pero el código solo desestructura `data` (ignora `error`), así que `alumno` siempre queda `null` — y la función devuelve siempre "No existe ninguna cuenta con ese email", **para cualquier email, incluso el de un alumno real ya registrado**. Este no es un caso "todavía no probado": es un caso "activamente da un mensaje de error falso todo el tiempo". Se necesita correr la migración `0003` (ver SQL abajo) para que funcione — no hay forma de arreglarlo desde el código, ya está bien escrito, depende 100% de la migración.
- [x] **Encontrado y arreglado en el camino**: la navegación admin → curso → "Gestionar clases" (donde vive la subida de video) estaba enterrada como uno de 6 botones idénticos en un sidebar — probado con un browser real logueado, siguiendo el mismo click que haría Ricardo, confirmando que este era el motivo real de "no encuentro dónde subir videos". Se agregaron botones directos "Clases y videos (N)" / "Accesos y ventas" en la lista de `/admin/cursos`, y un banner prominente en la página de editar. Verificado con click real de punta a punta: funciona.
- [x] **Investigado a fondo, corregido el diagnóstico inicial**: se pensó que login/logout (`router.push` + `router.refresh()`) se quedaban pegados en `/cuenta/login` por una condición de carrera. Con más pruebas (esperando hasta 20s en vez de 3s) se confirmó que **no era un bug de navegación** — el login siempre termina redirigiendo bien, pero en un cold start de Vercel puede tardar hasta ~8 segundos, y los tests anteriores no esperaban lo suficiente. Se dejó el cambio a navegación dura (`window.location.href`) igual, porque sigue siendo un patrón más simple y robusto para un redirect post-login (no depende de que el server vea la cookie de sesión en la próxima carga), pero el "bug" original era en realidad latencia de cold start, no una condición de carrera real — corregido acá para no dejar una conclusión equivocada en el registro.
- [x] Confirmado (sin bugs): reordenar clases (▲▼) funciona correctamente — sí cambia el orden real en la base; crear un curso nuevo redirige bien a su página de editar después de guardar.

**Para retomar rápido**: ver este bloque antes que nada. Los checkboxes sin marcar son exactamente lo que falta. Código de Partes C, D, E completo, F con Flow.cl completo (Stripe pausado a pedido de Ricardo, ver arriba), G arrancada. El sitio real está en producción en `https://pachicursos.vercel.app` (Vercel conectado al repo; el deploy automático en push todavía no se confirmó que ande solo, así que cada cambio de esta sesión se deployó a mano con `vercel --prod`). **Migraciones pendientes de correr en el proyecto real, en orden: `0002` (ya corrida), `0003`, `0004`.** 🔴 **`0003` ya no es "pendiente" — es un bloqueante confirmado: sin ella, otorgar acceso manual falla siempre, para cualquier alumno.** **Pendiente en Supabase**: agregar `https://pachicursos.vercel.app` a Authentication → URL Configuration → Redirect URLs — todavía no confirmado que esté hecho, y sin esto el login por link de confirmación/reset falla aunque el login normal con contraseña funcione bien. Pendientes de Parte A: DNS del subdominio real, cuenta Resend, trámite Flow.cl (este último bloquea probar un pago real y puede tardar días/semanas — conviene iniciarlo pronto, aunque no se toque el código de pagos mientras tanto). **🔴 Nuevo bloqueante (2026-09-12): el `VIMEO_ACCESS_TOKEN` actual no tiene el scope "upload" — hay que regenerarlo en developer.vimeo.com/apps con Upload+Edit+Private tildados y actualizarlo en `.env.local` y en Vercel (ver detalle en Parte D). Sin esto, subir un video real no va a funcionar para nadie.**

---

(el registro de eventos empieza acá — cada línea nueva se agrega debajo, nunca se edita una existente)
